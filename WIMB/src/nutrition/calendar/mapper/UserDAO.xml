<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="nutrition.calendar.imapper.IUserDAO">

	<!-- 사용자 아이디 중복 확인 -->
	<select id="idCheck" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM USER_INFO
		WHERE USER_ID = #{user_id}
	</select>
	
	<!-- 사용자 전화번호 중복 확인 -->
	<select id="telCheck" resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM USER_INFO
		WHERE TEL = #{user_tel}
	</select>
	
	<!-- 회원 등록 -->
	<insert id="join">
		INSERT INTO USER_INFO(USER_NUM, USER_NAME, TEL, USER_ID, USER_PW)
		VALUES(#{user_num}, #{user_name}, #{tel}, #{user_id}, CRYPTPACK.ENCRYPT(#{user_pw},#{user_pw}))
	</insert>
	
	<!-- 사용자가 설정한 칼로리 등록 -->
	<insert id="insertCal">
		INSERT INTO TOTAL_CAL(CAL_NUM, T_CALORIE, USER_NUM)
		VALUES(CALSEQ.NEXTVAL, #{t_caorie}, #{user_num})
	</insert>

</mapper>